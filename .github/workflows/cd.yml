name: CD

on:
  push:
    branches: [main]
    paths:
      - "package.json"
      - "apps/*/package.json"
      - "packages/*/package.json"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  detect-release:
    name: Detect Release Type
    runs-on: self-hosted
    outputs:
      should_deploy: ${{ steps.detect.outputs.should_deploy }}
      release_type: ${{ steps.detect.outputs.release_type }}
      backend_apps: ${{ steps.detect.outputs.backend_apps }}
      frontend_apps: ${{ steps.detect.outputs.frontend_apps }}
      app_versions: ${{ steps.detect.outputs.app_versions }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - id: detect
        run: |
          BACKEND=""
          FRONTEND=""
          BACKEND_VERSIONS=""
          FRONTEND_VERSIONS=""

          for app in api-service cron-service; do
            CURRENT=$(git show HEAD:apps/${app}/package.json 2>/dev/null | jq -r .version)
            PREVIOUS=$(git show HEAD~1:apps/${app}/package.json 2>/dev/null | jq -r .version)
            if [ "$CURRENT" != "$PREVIOUS" ]; then
              if [ -z "$BACKEND" ]; then
                BACKEND="\"${app}\""
                BACKEND_VERSIONS="${app}:${CURRENT}"
              else
                BACKEND="${BACKEND},\"${app}\""
                BACKEND_VERSIONS="${BACKEND_VERSIONS} ${app}:${CURRENT}"
              fi
            fi
          done

          for app in admin-web owl-web cron-web; do
            CURRENT=$(git show HEAD:apps/${app}/package.json 2>/dev/null | jq -r .version)
            PREVIOUS=$(git show HEAD~1:apps/${app}/package.json 2>/dev/null | jq -r .version)
            if [ "$CURRENT" != "$PREVIOUS" ]; then
              if [ -z "$FRONTEND" ]; then
                FRONTEND="\"${app}\""
                FRONTEND_VERSIONS="${app}:${CURRENT}"
              else
                FRONTEND="${FRONTEND},\"${app}\""
                FRONTEND_VERSIONS="${FRONTEND_VERSIONS} ${app}:${CURRENT}"
              fi
            fi
          done

          ALL_VERSIONS="${BACKEND_VERSIONS} ${FRONTEND_VERSIONS}"

          if [ -z "$BACKEND" ] && [ -z "$FRONTEND" ]; then
            echo "should_deploy=false" >> $GITHUB_OUTPUT
            echo "release_type=none" >> $GITHUB_OUTPUT
            echo "No version changes detected, skipping deployment."
          else
            CHANGED_APPS="$(echo "${BACKEND}${FRONTEND:+,$FRONTEND}" | tr -d '\"')"
            if [ "$CHANGED_APPS" = "api-service,cron-service,admin-web,owl-web,cron-web" ]; then
              RELEASE_TYPE="all"
            else
              RELEASE_TYPE="single"
            fi
            echo "should_deploy=true" >> $GITHUB_OUTPUT
            echo "release_type=${RELEASE_TYPE}" >> $GITHUB_OUTPUT
            echo "backend_apps=[${BACKEND}]" >> $GITHUB_OUTPUT
            echo "frontend_apps=[${FRONTEND}]" >> $GITHUB_OUTPUT
            echo "app_versions=${ALL_VERSIONS}" >> $GITHUB_OUTPUT
            echo "Release type: ${RELEASE_TYPE}"
            echo "Backend apps: ${BACKEND:-none}"
            echo "Frontend apps: ${FRONTEND:-none}"
            echo "Versions: ${ALL_VERSIONS}"
          fi

  verify:
    name: Verify and Lint
    runs-on: self-hosted
    needs: detect-release
    if: needs.detect-release.outputs.should_deploy == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install -g pnpm@10.12.4
      - run: pnpm install --frozen-lockfile
      - run: pnpm -F @owl/database prisma:generate
      - run: pnpm -F @owl/shared build
      - run: pnpm verify:quick

  build-backend:
    name: Build Backend
    runs-on: self-hosted
    needs: verify
    strategy:
      matrix:
        app: [api-service, cron-service]
    steps:
      - uses: actions/checkout@v4
      - name: Check if app needs build
        id: check-app
        env:
          APP: ${{ matrix.app }}
          BACKEND_APPS: ${{ needs.detect-release.outputs.backend_apps }}
        run: |
          if ! echo "$BACKEND_APPS" | grep -q "$APP"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Skipping $APP, not in changed apps"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi
      - name: Get version
        id: version
        if: steps.check-app.outputs.skip == 'false'
        run: |
          VER=$(jq -r .version apps/${{ matrix.app }}/package.json)
          echo "version=$VER" >> $GITHUB_OUTPUT
      - name: Build Docker image
        if: steps.check-app.outputs.skip == 'false'
        run: |
          docker build \
            -t owl/${{ matrix.app }}:${{ steps.version.outputs.version }} \
            -t owl/${{ matrix.app }}:latest \
            -f apps/${{ matrix.app }}/Dockerfile .

  build-frontend:
    name: Build Frontend
    runs-on: self-hosted
    needs: verify
    strategy:
      matrix:
        app: [admin-web, owl-web, cron-web]
    steps:
      - uses: actions/checkout@v4
      - name: Check if app needs build
        id: check-app
        env:
          APP: ${{ matrix.app }}
          FRONTEND_APPS: ${{ needs.detect-release.outputs.frontend_apps }}
        run: |
          if ! echo "$FRONTEND_APPS" | grep -q "$APP"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Skipping $APP, not in changed apps"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi
      - name: Get version
        id: version
        if: steps.check-app.outputs.skip == 'false'
        run: |
          VER=$(jq -r .version apps/${{ matrix.app }}/package.json)
          echo "version=$VER" >> $GITHUB_OUTPUT
      - name: Build Docker image
        if: steps.check-app.outputs.skip == 'false'
        run: |
          docker build \
            -t owl/${{ matrix.app }}:${{ steps.version.outputs.version }} \
            -t owl/${{ matrix.app }}:latest \
            -f apps/${{ matrix.app }}/Dockerfile .

  deploy:
    name: Deploy to K3s
    runs-on: self-hosted
    needs:
      - detect-release
      - build-backend
      - build-frontend
    if: always() && needs.detect-release.result == 'success' && (needs.build-backend.result == 'success' || needs.build-backend.result == 'skipped') && (needs.build-frontend.result == 'success' || needs.build-frontend.result == 'skipped')
    steps:
      - uses: actions/checkout@v4
      - name: Ensure namespace and deployments
        run: |
          docker exec k3s kubectl get namespace owl >/dev/null 2>&1 || docker exec k3s kubectl create namespace owl
          if ! docker exec k3s kubectl get deployment api-service -n owl >/dev/null 2>&1; then
            echo "Deployments not found, applying manifests..."
            docker cp k8s/base k3s:/tmp/k8s-base
            docker exec k3s kubectl apply -k /tmp/k8s-base/
            docker exec k3s rm -rf /tmp/k8s-base
          fi
      - name: Update existing deployment images
        env:
          APP_VERSIONS: ${{ needs.detect-release.outputs.app_versions }}
        run: |
          for entry in $APP_VERSIONS; do
            app="${entry%%:*}"
            ver="${entry##*:}"
            echo "Deploying ${app} version ${ver}..."
            docker exec k3s kubectl set image deployment/${app} ${app}=owl/${app}:${ver} -n owl
            docker exec k3s kubectl rollout status deployment/${app} -n owl --timeout=300s
          done
      - name: Verify deployment
        run: |
          docker exec k3s kubectl get pods -n owl
          docker exec k3s kubectl get services -n owl

  notify:
    name: Feishu Notify
    runs-on: self-hosted
    needs: deploy
    if: always()
    steps:
      - name: Send notification
        env:
          FEISHU_WEBHOOK: https://open.feishu.cn/open-apis/bot/v2/hook/d1555289-d3fa-4a54-995e-0dc088b7202c
          FEISHU_SECRET: Guh42RBEaDqLy38GIdmuxd
        run: |
          STATUS="${{ needs.deploy.result }}"
          RELEASE_TYPE="${{ needs.detect-release.outputs.release_type }}"
          VERSIONS="${{ needs.detect-release.outputs.app_versions }}"
          TIMESTAMP=$(date +%s)
          STRING_TO_SIGN="${TIMESTAMP}\n${FEISHU_SECRET}"
          SIGN=$(printf "${STRING_TO_SIGN}" | openssl dgst -sha256 -hmac "" -binary | base64)

          if [ "$STATUS" = "success" ]; then
            TITLE="Deploy Success"
            TEMPLATE="green"
            CONTENT="Release Type: ${RELEASE_TYPE}\nVersions: ${VERSIONS}\nCommit: ${{ github.sha }}\nTime: $(date '+%Y-%m-%d %H:%M:%S')"
          else
            TITLE="Deploy Failed"
            TEMPLATE="red"
            CONTENT="Release Type: ${RELEASE_TYPE}\nVersions: ${VERSIONS}\nCommit: ${{ github.sha }}\nTime: $(date '+%Y-%m-%d %H:%M:%S')\nPlease check workflow logs"
          fi

          curl -X POST "${FEISHU_WEBHOOK}" \
            -H "Content-Type: application/json" \
            -d "{
              \"timestamp\": \"${TIMESTAMP}\",
              \"sign\": \"${SIGN}\",
              \"msg_type\": \"interactive\",
              \"card\": {
                \"header\": {
                  \"title\": {\"tag\": \"plain_text\", \"content\": \"${TITLE}\"},
                  \"template\": \"${TEMPLATE}\"
                },
                \"elements\": [{
                  \"tag\": \"markdown\",
                  \"content\": \"${CONTENT}\"
                }]
              }
            }"
